# Cloudflare Pages configuration for the playground

name = "x402-proxy-playground"
compatibility_date = "2024-12-01"

# Build configuration
[build]
command = "npm run build"
output_directory = "dist"

# Testnet environment (Base Sepolia)
[env.testnet]
name = "x402-proxy-playground-testnet"

# Mainnet environment (Base)
[env.mainnet]
name = "x402-proxy-playground-mainnet"
